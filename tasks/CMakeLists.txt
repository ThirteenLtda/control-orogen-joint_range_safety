# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(joint_range_safetyTaskLib)
ADD_LIBRARY(${JOINT_RANGE_SAFETY_TASKLIB_NAME} SHARED 
    ${JOINT_RANGE_SAFETY_TASKLIB_SOURCES})
add_dependencies(${JOINT_RANGE_SAFETY_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${JOINT_RANGE_SAFETY_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${JOINT_RANGE_SAFETY_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${JOINT_RANGE_SAFETY_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${JOINT_RANGE_SAFETY_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${JOINT_RANGE_SAFETY_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${JOINT_RANGE_SAFETY_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${JOINT_RANGE_SAFETY_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${JOINT_RANGE_SAFETY_TASKLIB_HEADERS}
    DESTINATION include/orocos/joint_range_safety)

